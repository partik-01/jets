services:
  postgres:
    image: postgres:15-alpine
    container_name: aviation-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aviation_museum
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
