import Link from 'next/link';
import { aircraft } from '@/lib/aircraft-data';

export default function TimelinePage() { const exhibits = [...aircraft].sort((a,b) => a.introductionYear-b.introductionYear); return <section className="mx-auto w-full max-w-5xl px-6 py-16"><p className="font-mono text-xs tracking-[.2em] text-accent">CHRONOLOGY OF FLIGHT</p><h1 className="mt-2 font-heading text-5xl">A century of lift-off</h1><p className="mt-4 max-w-2xl text-muted-foreground">Follow the moments when an aircraft reshaped the limits of speed, reach, and imagination.</p><ol className="relative mt-12 border-l border-border pl-8">{exhibits.map((item) => <li key={item.slug} className="relative pb-12"><span className="absolute -left-[39px] top-1 h-5 w-5 rounded-full border-4 border-background bg-accent" /><p className="font-mono text-sm text-accent">{item.introductionYear}</p><Link href={`/aircraft/${item.slug}`} className="mt-1 block font-heading text-3xl hover:text-accent">{item.name}</Link><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">{item.description}</p><p className="mt-3 text-xs font-medium uppercase tracking-wide text-primary">{item.role}</p></li>)}</ol></section>; }
