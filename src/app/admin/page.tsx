import Link from 'next/link';
import { aircraft, categories } from '@/lib/aircraft-data';

export default function AdminPage() { return <section className="mx-auto w-full max-w-7xl px-6 py-16"><p className="font-mono text-xs tracking-[.2em] text-accent">CURATOR CONSOLE</p><h1 className="mt-2 font-heading text-5xl">Collection overview</h1><div className="mt-9 grid gap-5 sm:grid-cols-3">{[['Published exhibits', aircraft.length], ['Collections', categories.length], ['Featured entries', 3]].map(([label, value]) => <div key={label} className="rounded-2xl border border-border bg-card p-6"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-2 font-heading text-4xl text-primary">{value}</p></div>)}</div><div className="mt-10 rounded-2xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border p-6"><h2 className="font-heading text-2xl">Aircraft management</h2><Link href="/aircraft" className="text-sm text-accent">View public catalogue</Link></div><div className="divide-y divide-border">{aircraft.map((item) => <div className="flex items-center justify-between p-5" key={item.slug}><div><p className="font-medium">{item.name}</p><p className="text-xs text-muted-foreground">{item.category} · {item.status}</p></div><Link href={`/aircraft/${item.slug}`} className="text-sm text-primary hover:text-accent">Review</Link></div>)}</div></div><p className="mt-5 text-xs text-muted-foreground">This demo dashboard is ready to be connected to the included Prisma schema and credentials authentication.</p></section>; }
